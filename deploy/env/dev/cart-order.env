# Cart & Order Service Development Configuration

# Database Configuration
POSTGRES_PASSWORD=dev_password123
DB_HOST=order-db
DB_PORT=5432
DB_NAME=order_db
DB_USER=user
DB_PASSWORD=dev_password123

# Redis Configuration
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DATABASE=2
REDIS_TIMEOUT=2000

# Service Configuration
SERVER_PORT=8080
SERVICE_NAME=order-service

# Cart Configuration
CART_EXPIRATION_HOURS=24
CART_MAX_ITEMS=50
CART_CLEANUP_CRON=0 0 * * * *

# Order Configuration
ORDER_PREFIX=ORD
ORDER_STATUS_UPDATE_ENABLED=true
ORDER_HISTORY_RETENTION_DAYS=365
ORDER_CANCELLATION_WINDOW_MINUTES=10

# Inventory Check
INVENTORY_CHECK_ENABLED=true
INVENTORY_RESERVED_TIMEOUT_MINUTES=15

# Notification Configuration
NOTIFICATION_ENABLED=true
NOTIFICATION_ORDER_CREATED=true
NOTIFICATION_ORDER_CONFIRMED=true
NOTIFICATION_ORDER_DELIVERED=true

# Payment Integration
PAYMENT_SERVICE_URL=http://payment-service:8080
PAYMENT_TIMEOUT_SECONDS=30
PAYMENT_RETRY_ATTEMPTS=3

# Delivery Configuration
DELIVERY_ESTIMATE_ENABLED=true
DELIVERY_BASE_TIME_MINUTES=30
DELIVERY_DISTANCE_FACTOR=2

# Webhook Configuration
WEBHOOK_ENABLED=false
WEBHOOK_URL=http://localhost:3000/webhook
WEBHOOK_SECRET=dev-webhook-secret

# Logging
LOG_LEVEL=DEBUG
LOG_SQL=true
SHOW_SQL=true

# Development Features
SPRING_JPA_HIBERNATE_DDL_AUTO=update
SPRING_JPA_SHOW_SQL=true
SPRING_JPA_PROPERTIES_HIBERNATE_FORMAT_SQL=true

# Actuator
MANAGEMENT_ENDPOINTS_WEB_EXPOSURE_INCLUDE=*
MANAGEMENT_ENDPOINT_HEALTH_SHOW_DETAILS=always

# Test Mode Configuration (Dev only)
TEST_MODE_ENABLED=true
TEST_MODE_AUTO_CONFIRM_ORDERS=false
TEST_MODE_SKIP_PAYMENT=false
